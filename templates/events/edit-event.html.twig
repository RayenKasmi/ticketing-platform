{% extends 'base.html.twig' %}

{% block title %}Hello EventsController!{% endblock %}

{% block body %}
    <style>
        .events-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; margin-top: 200px}
        .events-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="events-wrapper">
        {{form_start(form)}}
            {{ form_widget(form) }}
        {{ form_end(form) }};
    </div>
{% endblock %}
